/** @type {import('next').NextConfig} */
const nextConfig = {
    // 如果你需要配置远程图片（比如来自 Supabase 或 Google），可以在这里加
    // images: {
    //   remotePatterns: [
    //     {
    //       protocol: 'https',
    //       hostname: 'your-project.supabase.co',
    //     },
    //   ],
    // },
  };
  
  export default nextConfig;